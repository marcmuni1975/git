gunicorn==21.2.0
```

El Procfile le indica a la plataforma de despliegue cómo ejecutar tu aplicación. En este caso:
- `web`: indica que es un proceso web
- `gunicorn`: el servidor WSGI que usaremos
- `app:app`: indica que el archivo principal es `app.py` y la instancia de Flask se llama `app`

Recuerda instalar gunicorn ejecutando:
```bash
pip install gunicorn
```// filepath: /Volumes/Programas/Proyectos/appweb/requirements.txt
gunicorn==21.2.0
```

El Procfile le indica a la plataforma de despliegue cómo ejecutar tu aplicación. En este caso:
- `web`: indica que es un proceso web
- `gunicorn`: el servidor WSGI que usaremos
- `app:app`: indica que el archivo principal es `app.py` y la instancia de Flask se llama `app`

Recuerda instalar gunicorn ejecutando:
```bash
pip install gunicorn